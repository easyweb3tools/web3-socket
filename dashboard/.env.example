# Dashboard Configuration
# This file contains example environment variables for the Socket.IO Dashboard

# API Configuration
# URL of the Socket.IO server API (if different from default)
# If not set, defaults to http://localhost:8081 (as configured in next.config.js)
API_URL=http://localhost:8081

# API Key for authenticating with the Socket.IO server
# This should match the API_KEY in your Socket.IO server's .env file
NEXT_PUBLIC_API_KEY=development_api_key

# Next.js Configuration
# Environment (development, production, test)
NODE_ENV=development

# Dashboard Port (when running in production)
PORT=3000

# Optional: Custom dashboard title/branding
NEXT_PUBLIC_APP_NAME=Socket.IO Dashboard
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Refresh intervals (in milliseconds)
NEXT_PUBLIC_DEFAULT_REFRESH_INTERVAL=10000
NEXT_PUBLIC_METRICS_REFRESH_INTERVAL=5000

# Optional: Dashboard features toggles
NEXT_PUBLIC_ENABLE_METRICS=true
NEXT_PUBLIC_ENABLE_LOGS=true
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# Optional: Theme configuration
NEXT_PUBLIC_THEME=light
NEXT_PUBLIC_PRIMARY_COLOR=#3B82F6